1 - Crie um programa que  gere senhas solicitadas com letras, números e símbolos  e que o usuário também escolha o tamanho da senha para criar senhas seguras automaticamente.

2 - Crie um programa que acesse a API para buscar um usuário específico aleatório. exibindo o nome, e-mail e país desse usuário, caso haja erro na conexão, mostre uma mensagem de falha.

3 - Crie um programa que consulte informações de uma API, retorne logradouro, bairro, cidade e estado do CEP digitalizado, caso o CEP não exista ou haja erro na requisição, mostre uma mensagem de falha.

4 - Crie um programa que realize consultas em relação ao Real (BRL) usando a API mostre valor atual, máximo, mínimo e data/hora da última atualização, caso a moeda não exista ou haja erro na requisição, retorne uma mensagem de erro.




1 - Crie um programa que lê um arquivo CSV de com a biblioteca , calcule e exiba a e o da coluna tempo_execucao, caso o arquivo não exista ou haja erro na leitura, mostre uma mensagem de erro. 


2 - Crie um programa que cria um arquivo com nome, idade e cidade de algumas pessoas, que este programa escreva os dados em formato tabular e salve o arquivo escolhido pelo usuário, caso ocorra um erro ao salvar, mostre uma mensagem de falha. 

3 - Crie um programa que leia um arquivo informado pelo usuário, percorrendo cada linha do arquivo e a exibindo na tela, caso o arquivo não seja encontrado, mostre uma mensagem de erro.

4 - Crie um programa que leia e escreva arquivos no formato , que salve em um dicionário com nome, idade e cidade em um arquivo JSON e depois leia o mesmo arquivo exibindo os dados, caso o arquivo não exista ou ocorra erro ao salvar, mostre uma mensagem de falha.